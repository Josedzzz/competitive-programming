#include <iostream>
#include <vector>
using namespace std;

int getCoins(vector<int> arr) {
  while (true) {
    for (int i = 0; i < arr.size(); i++) {
    }
  }
}

int main() {
  int t;
  cin >> t;
  while (t > 0) {
    int n;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; i++) {
      cin >> arr[i];
    }
  }
  return 0;
}
